#include "Shader.h"

Shader::Shader(const char* vertexPath, const char* fragmentPath) {
    // Load, compile and link shaders
    // This is a placeholder for shader compilation and linking logic
}

void Shader::use() { 
    // Use the compiled shader program
    // Placeholder for using the shader
}
