#include "PhysicsEngine.h"

void PhysicsEngine::update(std::vector<CelestialBody>& bodies, double deltaTime) {
    for (auto& body : bodies) {
        // Update the position and velocity of each celestial body
        // This is a placeholder for actual physics calculations
    }
}
